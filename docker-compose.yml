services:
  reviewer:
    build: .
    container_name: mr-rag-reviewer
    restart: unless-stopped
    env_file:
      - .env
    extra_hosts:
      - "gitlab.local:host-gateway"
    ports:
      - "8081:8081"
